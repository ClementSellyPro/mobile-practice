import { RootView } from "@/components/RootView";
import { ThemedText } from "@/components/ThemedText";

export default function favorites() {
  return (
    <RootView>
      <ThemedText>Favorites page</ThemedText>
    </RootView>
  );
}
